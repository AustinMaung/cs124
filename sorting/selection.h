#ifndef SELECTION_H// Prevent duplicate definition
#define SELECTION_H 

    int min_position(int a[], int from, int to);
    void swap(int& x, int& y);
    void selection_sort(int a[], int size);
    void print(int a[], int size);

#endif